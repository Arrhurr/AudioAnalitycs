# Recommandation

Dataset
We use the Universal Top Spotify Songs dataset from Kaggle, containing audio features and metadata for popular tracks across multiple countries.

Preprocessing
The dataset is cleaned by removing duplicates and missing values. Audio features are then standardized using z-score normalization to ensure fair distance computation.

venv\Scripts\activate
pip install pandas matplotlib seaborn scikit-learn jupyter ipykernel
python -m ipykernel install --user --name=music-env

![alt text](image.png)

🎯 Choix du modèle de recommandation
Problématique

L’objectif du projet est de concevoir un système de recommandation musicale capable de proposer des morceaux similaires à un titre donné à partir des caractéristiques audio fournies par Spotify.

Le dataset utilisé contient :

des informations audio (danceability, energy, valence, tempo, etc.)

des métadonnées (artiste, album, popularité, pays)

mais aucune donnée utilisateur (notes, historiques d’écoute, likes)

❌ Pourquoi ne pas utiliser un modèle supervisé ?

Les modèles supervisés nécessitent :

une variable cible (ex : note utilisateur)

un historique de préférences

Or, notre dataset ne contient pas ce type d’information.

Il est donc impossible de :

prédire une note

entraîner un modèle de classification ou de régression classique

appliquer un filtrage collaboratif

✅ Modèle choisi : Content-Based Filtering

Nous avons choisi d’implémenter un système de recommandation basé sur le contenu.

Principe

Deux morceaux sont considérés comme similaires si leurs caractéristiques audio sont proches.

Le système :

Représente chaque morceau comme un vecteur de features audio.

Normalise ces features.

Calcule une mesure de similarité entre morceaux.

Retourne les morceaux les plus proches d’un titre donné.

📐 Mesure de similarité : Similarité cosinus

La similarité cosinus mesure l’angle entre deux vecteurs dans un espace multidimensionnel.

Formule mathématique :

cos(A, B) = (A · B) / (||A|| ||B||)

Elle permet de :

comparer efficacement des données normalisées

mesurer la proximité relative entre morceaux

éviter les biais liés aux différences d’échelle

Cette mesure est couramment utilisée en :

recommandation

recherche d’information

traitement de données vectorielles

🎵 Features utilisées

Les caractéristiques audio sélectionnées sont :

danceability

energy

loudness

speechiness

acousticness

instrumentalness

liveness

valence

tempo

Ces features décrivent :

l’intensité

l’émotion

le caractère acoustique

la dynamique musicale

L’analyse de corrélation effectuée précédemment confirme leur pertinence.

🧠 Justification du choix

Le content-based filtering est adapté car :

Il ne nécessite pas de données utilisateurs.

Il exploite directement les caractéristiques musicales.

Il est interprétable.

Il est cohérent avec la nature du dataset.

Il permet une implémentation claire en programmation orientée objet.